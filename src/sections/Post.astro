---
import type { Lang } from "../i18n/ui";
import { useTranslations, formatDate, localizedPath } from "../i18n/utils";
import { readingTime, type Tutorial } from "../lib/content";

interface Props {
  post: Tutorial;
  lang: Lang;
}
const { post, lang } = Astro.props;
const t = useTranslations(lang);
const minutes = readingTime(post.body);
---

<article>
  <a
    href={localizedPath("/tutorials", lang)}
    class="font-mono text-sm text-brand-600 hover:underline dark:text-brand-400"
  >
    ← {t("post.back")}
  </a>

  <header class="mt-6">
    <div class="flex flex-wrap items-center gap-2 font-mono text-xs text-gray-500 dark:text-gray-400">
      <time datetime={post.data.pubDate.toISOString()}>
        {formatDate(post.data.pubDate, lang)}
      </time>
      <span>·</span>
      <span>{minutes} {t("post.minRead")}</span>
      {
        post.data.tags.length > 0 && (
          <>
            <span>·</span>
            <span class="text-brand-600 dark:text-brand-400">
              {post.data.tags.map((tag) => `#${tag}`).join(" ")}
            </span>
          </>
        )
      }
    </div>
    <h1
      class="mt-3 text-3xl font-bold leading-tight tracking-tight text-gray-900 dark:text-white sm:text-4xl"
    >
      {post.data.title}
    </h1>
    <p class="mt-3 text-lg text-gray-600 dark:text-gray-400">
      {post.data.description}
    </p>
  </header>

  <div class="prose mt-10 max-w-none text-gray-700 dark:text-gray-300">
    <slot />
  </div>
</article>
